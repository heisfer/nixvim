{
  plugins = {
    treesitter = {
      enable = true;
      ensureInstalled = "all";
    };

    treesitter-context = {
      enable = true;
    };

    treesitter-refactor = {
      enable = true;
    };

    treesitter-textobjects = {
      enable = true;
    };
    ts-autotag = {
      enable = true;
    };
  };
}
